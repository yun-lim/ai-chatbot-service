.env
.env.*
!.env.example
.vercel/
*.db
*.sqlite3
data/
__pycache__/
*.py[cod]
.venv/
venv/
logs/
*.log
.DS_Store
.idea/
.vscode/

# `vercel build` 가 requirements.txt 와 .python-version 에서 자동 생성하는 산출물.
# 커밋하면 의존성 정의가 requirements.txt 와 두 갈래로 갈라진다.
# 나중에 pyproject.toml 을 직접 쓰기로 하면 이 두 줄을 지울 것.
pyproject.toml
uv.lock

# graphify 산출물 (로컬 분석용)
graphify-out/

# 다이어그램 뷰어 검사 스크린샷·리포트 (로컬 확인용). README 의 PNG 는 뷰어 UI 없이 따로 뽑는다.
docs/architecture/*.visual-check.*
